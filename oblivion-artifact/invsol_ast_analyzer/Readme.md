Go inside invsol_ast_analyzer folder. Then run command:

python -m invsol_ast.cli examples/contracts/SimpleToken.sol   --out examples/outputs/simpletoken_ir.json   --print


Thats all that you have to do.
